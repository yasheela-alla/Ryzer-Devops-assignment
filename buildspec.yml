version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install -g npm@9
      - npm ci --legacy-peer-deps
  build:
    commands:
      - npm run build
      - npm run export
artifacts:
  files:
    - '**/*'
  base-directory: out

